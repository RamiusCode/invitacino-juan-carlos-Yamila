---
// HeroSection.astro
---

<section class="relative w-full h-screen flex flex-col items-center justify-center overflow-hidden">
  <!-- Imagen de fondo -->
  <div class="absolute inset-0 z-0">
    <img 
      src="/imagenes/Foto1.webp" 
      alt="Juan Carlos y Yamila" 
      class="w-full h-full object-cover"
    />
    <!-- Overlay oscuro para mejorar legibilidad -->
    <div class="absolute inset-0 bg-black/40"></div>
  </div>

  <!-- Contenido principal -->
  <div class="relative z-10 text-center px-6 flex flex-col items-center justify-center h-full">
    
    <!-- Iniciales decorativas en la parte superior -->
    <div class="mb-8">
      <div class="w-20 h-20 rounded-full border-2 border-white/80 flex items-center justify-center backdrop-blur-sm bg-white/10">
        <span class="font-charm text-4xl text-white">JY</span>
      </div>
    </div>

    <!-- Texto "NUESTRA BODA" -->
    <p class="text-white/90 text-sm md:text-base tracking-[0.3em] uppercase mb-12 font-light">
      Nuestra Boda
    </p>

    <!-- Nombres de los novios -->
    <div class="my-8 space-y-4">
      <h1 class="font-great-vibes text-5xl md:text-7xl lg:text-8xl text-white drop-shadow-2xl leading-tight">
        Juan Carlos
      </h1>
      
      <!-- Separador con & -->
      <div class="flex items-center justify-center gap-4 py-2">
        <div class="h-px w-12 md:w-16 bg-white/60"></div>
        <span class="font-charm text-4xl md:text-5xl text-white/90">&</span>
        <div class="h-px w-12 md:w-16 bg-white/60"></div>
      </div>
      
      <h1 class="font-great-vibes text-5xl md:text-7xl lg:text-8xl text-white drop-shadow-2xl leading-tight">
        Yamila
      </h1>
    </div>

    <!-- Fecha de la boda -->
    <div class="mt-12 flex items-center gap-3 px-6 py-3 rounded-full border border-white/40 backdrop-blur-sm bg-white/10">
      <!-- Icono de calendario -->
      <svg class="w-5 h-5 text-white/80" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
      </svg>
      
      <p class="text-white text-base md:text-lg tracking-wider font-light">
        20 diciembre 2025
      </p>
      
      <!-- Icono decorativo derecho -->
      <svg class="w-5 h-5 text-white/80" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
      </svg>
    </div>

  </div>

  <!-- Indicador de scroll (opcional) -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2 z-10 animate-bounce">
    <svg class="w-6 h-6 text-white/60" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
    </svg>
  </div>
</section>

<style>
  /* Animaciones */
  section {
    animation: fadeIn 1.5s ease-in;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  h1 {
    animation: slideUp 1s ease-out;
    text-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
  }

  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Mejorar legibilidad en móviles */
  @media (max-width: 640px) {
    h1 {
      text-shadow: 0 2px 10px rgba(0, 0, 0, 0.7);
    }
  }
</style>